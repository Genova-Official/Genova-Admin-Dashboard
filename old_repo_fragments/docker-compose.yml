version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/.next
      - /app/node_modules
    command: npm run start
